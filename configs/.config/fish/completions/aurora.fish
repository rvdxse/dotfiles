complete -c aurora -f -a pacman-search -d "Search and install pacman packages"

complete -c aurora -f -a pacman-remove -d "Delete pacman packages"
complete -c aurora -f -a system-update -d "Update all packages"
complete -c aurora -f -a yay-search -d "Search and install AUR packages"
complete -c aurora -f -a yay-remove -d "Delete packages from AUR"
complete -c aurora -f -a pacman-orphans -d "Find and remove orphaned packages"
complete -c aurora -f -a pacman-cache -d "Clean pacman package cache"
complete -c aurora -f -a system-info -d "Show system overview"
